# Front-end / Public Variables Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-jwt-anon-key-here

# Backend / Secure Configuration
VITE_ADMIN_EMAILS=your.email@example.com
PORT=3000

# DO NOT put the SUPABASE_SERVICE_ROLE_KEY here unless strictly running backend in isolated environment.
# Ensure any secret keys remain on the backend deployment (e.g. Render) ONLY.
